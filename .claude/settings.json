{
	"hooks": {
		"PreToolUse": [
			{
				"matcher": "Edit|Write|MultiEdit|Bash",
				"hooks": [
					{
						"type": "command",
						"command": "cd \"${CLAUDE_PROJECT_DIR}\" && . ./.lefthookrc && { command -v node >/dev/null 2>&1 || { echo 'protection hook: node is not on PATH' >&2; exit 2; }; } && node .claude/hooks/protection.ts"
					}
				]
			}
		]
	}
}
